#!/bin/sh
# Block commits directly to main. Enable with: git config core.hooksPath .githooks
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo "Abort: commit to main is blocked. Create a feature branch (feat/<topic> or fix/<topic>)." 1>&2
  exit 1
fi

